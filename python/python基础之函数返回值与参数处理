1、函数返回值
    （1）函数返回：return 1,2,3,4,5
        接收时，则 a,b,c,d,e = 函数
        或者nums = 函数，nums[1],nums[2],nums[3],nums[4],nums[5]

    （2）函数同时多个return，只能执行一个return，即第一个。
        除非有条件if n==1:
                             return 1
                          elif n ==2:
                             return 2
                          else:
                              return 3
2、函数传递
    位置和类型，必须保持一致。否则会抛出类型错误

3、位置参数

4、关键字参数

5、默认值参数

6、可变参数

7、混合参数
